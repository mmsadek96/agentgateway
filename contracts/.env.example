# Your MetaMask wallet private key (the one with $120 ETH on Base)
# NEVER commit the actual .env file — only this example
PRIVATE_KEY=0xyour_private_key_here

# BaseScan API key for contract verification (get free at https://basescan.org/apis)
BASESCAN_API_KEY=your_basescan_api_key_here
