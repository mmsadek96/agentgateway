# AgentTrust Station API URL
STATION_URL=https://agentgateway-6f041c655eb3.herokuapp.com

# Must match addon-manifest.json api.password
ADDON_PASSWORD=REPLACE_WITH_SECRET

# Must match addon-manifest.json api.sso_salt
SSO_SALT=REPLACE_WITH_SSO_SALT

# Server port (Heroku sets this automatically)
PORT=5000
